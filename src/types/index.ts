// Placeholder data model for the read-only `items` collection.
// Replace these fields with the real shape once the domain is defined.
export interface Item {
  id: string;
  title: string;
  description?: string;
  createdAt?: number;
}
